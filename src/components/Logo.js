import classes from './css/logo.module.css'

// todo top left in orb
// todo add pointer to mouse on hover
const logo = () => {
    return (
        <div>
            <img src="/artwork/logo.png" alt="logo" width={100} />
        </div>
    )
}

export default logo
